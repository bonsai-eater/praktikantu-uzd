Contribution guide:
This repo uses mkdocs, so use markdown when editing the files. To add new files or change file names, you will need to  update the nav.yml, as this repo uses https://github.com/lukasgeiter/mkdocs-awesome-nav

Once pushed, the changes will automatically deploy to the server.