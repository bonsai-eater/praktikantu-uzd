---
weight: 200
---
# Server SSH hardening
- Create a new user with root permissions
- Create a new ssh key and add it to the newly created user
- Restrict the root user from connecting via SSH.
- Change SSH to a non-standard port